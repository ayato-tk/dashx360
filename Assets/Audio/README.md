Drop replacement WAV files here with these names:

- focus.wav
- select.wav
- back.wav
- tab.wav
- startup.wav

If a file is missing, the app falls back to a quiet system sound so the UI still has feedback during development.
